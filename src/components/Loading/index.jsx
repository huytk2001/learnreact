import React from 'react';
import PropTypes from 'prop-types';
import './style.scss'
Loadng.propTypes = {
    
};

function Loadng(props) {
    return (
        <div>
            
        </div>
    );
}

export default Loadng;